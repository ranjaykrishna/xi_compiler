use cp
use oo


class MultiColoredPoint extends ColoredPoint{
	pixel:int
	wtf:bool
	
	initMultiColoredPoint(x0:int, y0:int, col:Color, p:int, w:bool):MultiColoredPoint{
		_ = initColoredPoint(x0, y0, col);
		pixel = p;
		wtf = w;
		return this;
	}
}